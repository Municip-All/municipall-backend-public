# --- Application Configuration ---
NODE_ENV=development
PORT=3000

# --- Database (PostGIS) ---
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=password
DATABASE_NAME=municipall

# --- Cache & Queues (Redis) ---
REDIS_HOST=localhost
REDIS_PORT=6379

# --- Authentication ---
JWT_SECRET=0abfioeafoipahpfiahipfhapifipahfipahip

# --- AI Integrations ---
OPENAI_API_KEY=sk-fouhezohfoiebfoeafpoiahfpiahfpaoiahfo

# --- Météo (OpenWeatherMap) ---
# Clé gratuite : https://home.openweathermap.org/api_keys
OPENWEATHER_API_KEY=

# --- Notifications push (Expo) ---
# Les alertes backoffice utilisent https://exp.host (aucune clé requise pour Expo Push).
# L'app mobile enregistre un ExponentPushToken via POST /users/push-token.
